---
// Webflow's edge 301-strips trailing slashes (/docs/ → /docs).
// Cloudflare Workers Static Assets 307-adds them back for any
// prerendered index.html (/docs → /docs/). The two fight forever.
//
// This SSR page replaces the prerendered index so there is no static
// index.html for the assets layer to match. The redirect runs inside
// the worker, bypassing the loop entirely.
//
// Raw Response avoids Astro's base-prefix logic (Webflow Cloud may
// inject base: '/docs' at build time — we can't be sure).
return new Response(null, {
	status: 302,
	headers: { Location: '/docs/home' },
});
---
